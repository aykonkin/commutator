{
  "name": "fastchain-contracts",
  "version": "0.0.1",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "truffle compile --all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "truffle": "4.1.14"
  }
}
